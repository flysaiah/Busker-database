GAMEPLAN:
--Search for nonexistent performer link still pops up (problem)
--Sign up with an already-used username (problem)
--Error messages not showing anywhere
--Links to everything
--Display "Hello, <User>" on frontpage
--Display flash messages



return Concert.query.join(favorites, (favorites.c.p_email == Performer.performer_email)).filter(favorites.c.u_email == self.user_email)

